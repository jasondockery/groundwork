# Claude Code

Claude Code reads `CLAUDE.md`; every other agent reads `AGENTS.md`. This file is
a thin adapter so both load the same instructions. Keep repository policy in
`AGENTS.md` — do not add rules here unless they are genuinely Claude-only.

@AGENTS.md
