# Groundwork-managed browser selection. The main Brewfile is installed with
# --require-sha; this separate manifest documents the exact official Chrome
# casks handled by the announced no-checksum exception lane.
{{ includeTemplate "browser-brewfile" . -}}
