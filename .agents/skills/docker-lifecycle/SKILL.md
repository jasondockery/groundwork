---
name: docker-lifecycle
description: "Keep local Docker verification builds disposable and cleanup bounded. Use before changing a Dockerfile, running a local image build for Groundwork, changing Docker build or cleanup helpers, or documenting an agent Docker workflow."
---

# Docker Lifecycle

> GENERATED by scripts/generate-skill-adapters.mjs — do not edit.

**Canonical workflow: [`skills/docker-lifecycle/SKILL.md`](../../../skills/docker-lifecycle/SKILL.md).**

Read that file and follow it. This adapter exists only so the skill is
discovered natively by this tool; the workflow itself is maintained in one
place so the two trees cannot drift apart.
